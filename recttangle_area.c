// program to find the area of rectangle

#include <stdio.h>

int main ()
{
    float length, width, area;
    length=4.4;
    width=3.5;
    area=length*width;

    printf ("Area of rectangle is : %f \n",area);
    return 0;
}
